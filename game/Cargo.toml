[package]
name = "game"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", default-features = false }
bevy_ggrs = "0.16"


[dev-dependencies]
bevy = { version = "0.14", default-features = true }